<!--pages/cart/cart.wxml-->
<wxs module="fn" src="../../utils/fn.wxs"></wxs>

<view class="container">
    <van-nav-bar title="报名" border="{{ false }}" z-index="{{ 21 }}" custom-class="nav" class="van-nav" placeholder fixed title-class="nav-title">
        <view slot="left" class="leftIcon" bindtap="onPop">
            <text class="iconfont icon-fanhuiiblack"></text>
        </view>
    </van-nav-bar>
    <view class="blank">
        <view class="icon" bindtap="onClickShare">
            <image class="fullimg" src="/packageTeamwork/static/teamwork_check.png"></image>
        </view>
        <view class="text">提交成功，正在审核中…</view>
        <view class="btn-wrap">
            <van-button custom-class="big-btn" bind:click="gotoList" round type="primary" size="large">
                返回团作列表
            </van-button>
        </view>
    </view>
</view>
